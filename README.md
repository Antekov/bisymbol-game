# bisymbol-game
Игра про слова на две последние буквы

# 🧠 Игра по словам

**«Игра по словам»** — это лингвистическая игра на сообразительность и словарный запас. Игрок и компьютер по очереди называют слова, соблюдая строгие правила перехода. Цель — набрать как можно больше баллов и пройти как можно больше уровней!

---

## 📜 Основные правила

1. **Начало игры**  
   Компьютер выбирает случайное **нарицательное существительное** в именительном падеже, единственном числе, и показывает его игроку.

2. **Ход игрока**  
   Игрок должен ввести **новое слово**, которое:
   - является нарицательным существительным (единственное число, именительный падеж),
   - **начинается с двух последних букв** предыдущего слова,
   - **ещё не использовалось** в текущей игре.

   **Пример**:  
   Компьютер: **кот** → игрок может ввести **отец**, **отлив**, **отчёт** и т.д.

3. **Аварийные ходы** (если слов на нужную пару букв нет или игрок не может вспомнить):
   - **Вариант A**: слово на **переставленные две последние буквы**  
     (например, вместо `от` → `то`).  
     → Баллы за слово **уменьшаются в 2 раза**.
   - **Вариант B**: слово на **последнюю букву**  
     (например, вместо `от` → `т...`).  
     → Баллы за слово **уменьшаются в 4 раза**.

   > 💡 Аварийные ходы разрешены **только если нет корректного слова** на исходную пару букв. В MVP эта проверка может быть упрощена.

4. **Ход компьютера**  
   После успешного хода игрока компьютер автоматически подбирает своё слово по тем же правилам.  
   Если подходящих слов нет — игра **не завершается**, а просто передаёт ход игроку с новыми условиями (без штрафа).

5. **Завершение игры**  
   Игра заканчивается, если игрок:
   - не может ввести слово (включая аварийные варианты) за отведённое время,
   - вводит недопустимое слово (не из словаря, уже использованное, несуществительное).

---

## 🎯 Система баллов

- Каждое слово оценивается по **биграммам** (последовательным парам букв).
- Чем **реже** встречается биграмма в русском языке — тем **больше** она даёт баллов.
- Балл за слово = сумма баллов всех его биграмм:
  \[
  \text{score}(w) = \sum_{i=1}^{n-1} \frac{1}{\text{частота}(w_i w_{i+1}) + 0.001}
  \]
- При использовании аварийных вариантов итоговый балл умножается на:
  - `0.5` — при перестановке букв,
  - `0.25` — при использовании одной буквы.

---

## 📈 Уровни и прогресс

- **Уровень 0**: время не ограничено.
- **Уровни 1+**: вводится ограничение по времени на ход (от 5 минут до 20 секунд).
- Чем выше уровень — тем меньше времени.
- За прохождение каждого уровня (начиная с 1-го) начисляются **дополнительные бонусные баллы**.
- Цель игрока — **максимизировать общий счёт** и **достичь максимального уровня**.

---

## 🧩 Технические детали

- Используется словарь русских нарицательных существительных.
- Биграммы и их частоты рассчитываются на основе корпуса русского языка.
- Игра реализована как веб-приложение на **Flask** с интерфейсом на **Bootstrap**.

---

## ▶️ Как начать

1. Установите зависимости:
   ```bash
   pip install flask
